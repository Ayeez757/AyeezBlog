# AyeezBlog 性能分析报告（JMeter）

## 1. 报告目标

本报告用于对 `AyeezBlog` 后端核心接口进行系统化性能评估，覆盖：

- 公网高频读取链路（文章列表、详情、相册、友链、关于页）
- 访客埋点链路（`/post/stats/track`）
- 管理端看板链路（登录、仪表盘、日志列表、文章列表）

最终目标：

- 评估当前系统在典型业务负载下的吞吐能力与稳定性边界
- 提前识别数据库、应用线程池、网络 IO 等潜在瓶颈
- 形成可复用的基线报告与扩容/优化依据

---

## 2. 测试资产与目录

- JMeter 脚本：`performance/jmeter/ayeezblog-performance-test.jmx`
- 报告文档：`performance/docs/AyeezBlog-性能分析报告.md`
- 结果文件（执行后生成）：`performance/jmeter/results/ayeezblog-perf.jtl`
- HTML 可视化报告（执行后生成）：`performance/jmeter/reports/html`

### 2.1 被测服务器规格（公网压测基线）


| 项目   | 规格                                                            |
| ---- | ------------------------------------------------------------- |
| CPU  | **2 核**                                                       |
| 内存   | **4 GB**                                                      |
| 公网带宽 | **峰值约 5 Mbps**（与 `AyeezBlog-性能指标标准.md` 第 1.1 节一致；具体以云控制台单位为准） |


---

## 3. 测试方法论（专业实践）

本次方案采用“基线 -> 压力 -> 诊断 -> 优化回归”的工程化路径：

1. **基线测试（Baseline）**
  在可控并发下运行 15 分钟，观察响应时间分布、错误率和资源占用。
2. **容量验证（Load）**
  逐步提高并发，定位吞吐拐点（TPS 不再线性增长、P95 快速恶化处）。
3. **稳定性测试（Soak）**
  中高并发持续 1~2 小时，验证连接池、线程池、缓存和数据库的长期稳定性。
4. **突刺测试（Spike）**
  短时间骤增并发，验证系统弹性、限流与恢复能力。

---

## 4. 测试模型设计

## 4.1 线程组与业务映射

### TG01-Public-ReadAndTrack-Baseline（开启）

- 并发用户：80
- Ramp-Up：120 秒
- 持续时长：900 秒（可调）
- 核心请求：
  - `GET /post/list`
  - `GET /post/get`
  - `GET /about/anime/list`
  - `GET /links/list`
  - `GET /album/list`
  - `POST /post/stats/track`

说明：该线程组模拟真实前台用户浏览和埋点上报，是最重要的公网负载模型。

### TG02-Admin-Dashboard-Baseline（开启）

- 并发用户：20
- Ramp-Up：60 秒
- 持续时长：900 秒
- 核心请求：
  - `POST /admin/login`（每虚拟用户仅登录一次）
  - `GET /admin/stats/dashboard`
  - `GET /admin/post/list`
  - `GET /admin/logs/list`

说明：覆盖管理端运营场景，关注接口聚合查询和看板峰值响应。

### TG03-Admin-Write-Scenario-DisabledByDefault（默认关闭）

- 用于后续扩展写操作压测（避免误写生产环境数据）
- 建议仅在预发布/压测专用环境开启

## 4.2 专业压测配置点

- 统一 HTTP Defaults（超时、主机、端口）
- Header/Cookie/Cache 管理器
- Uniform Random Timer（模拟人类停顿）
- JSON 提取器（从列表中提取文章 ID，用于详情链路）
- 响应断言（校验 `code=200`，避免“假成功”）

---

## 5. 执行步骤

## 5.1 前置准备

1. 启动被测服务（默认 `http://127.0.0.1:8080`）
2. 准备压测专用账号（管理端）
3. 确认数据库、Redis（若有）、MQ（若有）等依赖稳定
4. 清理旧结果目录（避免报告混杂）

## 5.2 命令行执行（推荐）

在项目根目录执行：

```bash
jmeter -n -t performance/jmeter/ayeezblog-performance-test.jmx ^
  -Jprotocol=http ^
  -Jhost=127.0.0.1 ^
  -Jport=8080 ^
  -Jadmin_username=admin ^
  -Jadmin_password=你的密码 ^
  -Jramp_up_sec=120 ^
  -Jhold_sec=900 ^
  -l performance/jmeter/results/ayeezblog-perf.jtl ^
  -e -o performance/jmeter/reports/html
```

> Windows PowerShell 可将 `^` 改为反引号 ```，或写成单行命令。

## 5.3 生产环境零污染执行规范（必读）

当前 `jmx` 已调整为默认零污染模式：

- `TG01-Public-ReadOnly-Baseline`：仅保留只读 `GET` 请求
- `Track /post/stats/track`：默认 `enabled=false`（不会增加 PV/UV）
- `TG02-Admin-Dashboard-Baseline`：默认 `enabled=false`（避免登录链路产生业务副作用）
- `TG03-Admin-Write-Scenario-DisabledByDefault`：默认 `enabled=false`

生产压测时请保持以上默认状态，不要启用任何 `POST/PUT/DELETE` 采样器或线程组。

## 5.4 图表可视化（更直观）

统一使用 **JMeter 官方 HTML Dashboard** 查看图表：

1. 执行压测时增加参数：
  - `-e -o performance/jmeter/reports/html`
2. 压测结束后打开：
  - `performance/jmeter/reports/html/index.html`

官方 Dashboard 可直接查看：

- 请求吞吐趋势（Throughput）
- 响应时间分布与百分位（Percentiles）
- 错误率与错误明细
- 各接口统计对比

---

## 6. 指标口径与验收标准

完整分级阈值、场景分组与阶梯压测门禁见：`performance/docs/AyeezBlog-性能指标标准.md`。

## 6.1 核心指标

- 吞吐量：TPS / RPS
- 时延：Avg / P90 / P95 / P99
- 错误率：HTTP 非 2xx + 业务断言失败
- 资源：CPU、Heap、GC、线程数、数据库连接池使用率

## 6.2 建议 SLA（可按业务调整）

- 接口错误率 `< 0.5%`
- 核心读接口 P95 `< 300ms`
- 管理端看板接口 P95 `< 800ms`
- 在目标并发下 15 分钟内无持续性抖动与雪崩

---

## 7. 测试结果记录（分拓扑基线）

> 数据来源：  
>
> - 原始结果：`performance/jmeter/results/server-full-readonly.jtl`  
> - 官方图表：`performance/jmeter/reports/server-full-readonly/index.html`

### 7.0 测试拓扑口径说明（避免混淆）

本章包含两类拓扑，结论需分开阅读：

1. **7.1~7.4**：`本地后端 -> 远程数据库`（应用在本机运行）
2. **7.5（后续阶梯/精测）**：`本地 JMeter -> 云服务器公网服务（https://blog.ayeez.cn/api）`

除 7.1~7.4 外，后续阶梯压测与 S 档测定均按第 2 条口径执行。

### 7.1 本轮压测配置

- 目标地址：`http://127.0.0.1:8080/api`（本机后端实例）
- 负载模型：`TG01-Public-ReadOnly-Baseline`
- 配置参数：`threads=80`，`ramp_up_sec=120`，`hold_sec=900`
- 数据保护：`track` 写入关闭、管理端线程组关闭、写场景线程组关闭
- 数据访问形态：应用部署在本地，数据库为远程实例（跨网络访问）

### 7.2 全局结果


| 指标     | 数值                  |
| ------ | ------------------- |
| 测试开始时间 | 2026-04-15 02:07:57 |
| 测试结束时间 | 2026-04-15 02:22:56 |
| 总时长    | 898.91 s            |
| 总请求数   | 50,515              |
| 成功请求数  | 49,323              |
| 失败请求数  | 1,192               |
| 全局错误率  | 2.36%               |
| 全局吞吐量  | 56.20 req/s         |
| 平均响应时间 | 377.91 ms           |
| P50    | 142 ms              |
| P90    | 310.6 ms            |
| P95    | 1169 ms             |
| P99    | 5002 ms             |
| 最大响应时间 | 38329 ms            |


### 7.3 接口维度结果（本地后端 + 远程数据库）


| 接口                      | 请求数    | 错误率   | 平均(ms) | P95(ms) | P99(ms) | 最大(ms) | 吞吐(req/s) |
| ----------------------- | ------ | ----- | ------ | ------- | ------- | ------ | --------- |
| `GET /post/list`        | 10,133 | 2.24% | 365.03 | 1160.0  | 5002.0  | 19329  | 11.27     |
| `GET /post/get`         | 10,119 | 2.35% | 378.03 | 1195.6  | 5002.0  | 19319  | 11.29     |
| `GET /about/anime/list` | 10,103 | 2.30% | 368.50 | 1163.9  | 5002.0  | 5021   | 11.28     |
| `GET /links/list`       | 10,087 | 2.56% | 396.63 | 1529.9  | 5002.0  | 38329  | 11.27     |
| `GET /album/list`       | 10,073 | 2.35% | 381.43 | 1167.4  | 5002.0  | 8182   | 11.27     |


### 7.4 错误分布与根因判断

主要错误类型为超时类异常，且在所有只读接口上均有分布：

- `ConnectTimeoutException`（主导错误）
- 少量 `SocketTimeoutException: Read timed out`

根因倾向：

1. **本地应用 -> 远程数据库链路优先排查**：在应用本机压测条件下仍出现连接/读超时，且接口无明显业务偏向，说明瓶颈更可能位于应用到数据库的网络链路、连接池或数据库侧容量，而非公网入口本身。
2. **应用线程池与 DB 连接池在高并发下可能互相放大**：当远程 DB 响应抖动时，应用线程等待连接或慢查询返回，容易形成排队与超时连锁。
3. **慢请求长尾显著**：P99 接近 5s、最大值达 38s，说明存在明显尾延迟；在该测试拓扑下应优先把其解释为“远程数据访问长尾 + 应用侧排队效应”。

> 口径更正：本节（7.1~7.4）属于“**本地后端 + 远程数据库**”基线，不应直接等同于“公网域名整链路”压测结论。

---

### 7.5 公网只读分级精测结果（A/B/S，2026-04-15）

为评估 **G1 公网只读 A/B/S 分级下的可达并发区间**，在同一公网目标（`https://blog.ayeez.cn/api`）、只读脚本与 `**hold_sec=900`、`ramp_up_sec=120`** 下，对多档并发进行了长稳态精测。压测发起端为 **本地 JMeter**（与 `AyeezBlog-性能指标标准.md` 第 1.2 节「压测发起位置」一致）。

> 说明：本节涵盖的 `5/10/20/40/60/63` 等后续阶梯压测，均为“**本地 JMeter -> 云服务器公网服务**”路径，不属于 7.1~7.4 的本地后端拓扑。

按 `AyeezBlog-性能指标标准.md` 第 4.1 节口径：

- **G1-A**：错误率 `<=0.5%`，G1 P95 `<=300ms`，G1 P99 `<=1500ms`
- **G1-B**：错误率 `<=2.0%`，G1 P95 `<=800ms`，G1 P99 `<=3000ms`
- **G1-S**：错误率 `<=0.1%`，G1 P95 `<=200ms`，G1 P99 `<=800ms`

#### 7.5.1 全局汇总（Dashboard Statistics / Total）

下列百分位取自官方 HTML 报告 **Statistics** 表中 **Total** 行（列名多为 90th / 95th / 99th，与 P95/P99 对应）。


| threads | 总请求数   | 全局错误率  | Total 约 P95 (ms) | Total 约 P99 (ms) | G1-A             | G1-B         | G1-S             |
| ------- | ------ | ------ | ---------------- | ---------------- | ---------------- | ------------ | ---------------- |
| 5       | 3,628  | 0%     | 675              | 1168             | 不满足（P95）         | 满足           | 不满足（P95）         |
| 10      | 7,347  | 0.136% | 414              | 1152             | 不满足（P95）         | 满足           | 不满足（错误率+P95）     |
| 20      | 14,063 | 0.043% | 723              | 1560             | 不满足（P95+P99）     | 满足           | 不满足（P95）         |
| 40      | 29,021 | 0.014% | 503              | 1154             | 不满足（P95）         | 满足           | 不满足（P95）         |
| 60      | 43,033 | 0.056% | 1131             | 1171             | 不满足（P95）         | 不满足（P95）     | 不满足（P95）         |
| 63      | 43,726 | 0.151% | 1140             | 3152             | 不满足（错误率+P95+P99） | 不满足（P95+P99） | 不满足（错误率+P95+P99） |


> 注：`5～40` 行总请求数与百分位来自本仓库 `performance/jmeter/reports/s-tier-fine-threads{5,10,20,40}-h900/statistics.json` 中 **Total**；`60/63` 来自同期精测归档（同目录若未提交可仅保留结论表）。

#### 7.5.2 分级结论（A/B/S）

基于第 7.5.1 表（以 Dashboard Total 先行判档）：

1. **G1-A：0/6 档通过**（`5/10/20/40/60/63` 全部不满足，主因是 P95>300ms）。
2. **G1-B：4/6 档通过**（`5/10/20/40` 通过；`60/63` 不通过，主因是 P95，且 `63` 同时败于 P99）。
3. **G1-S：0/6 档通过**（延迟阈值最严，全部不满足）。

在接口维度上，五个 G1 只读接口的 P95 普遍高于 A/S 阈值，与 Total 判档趋势一致；若要作为正式容量承诺，仍建议按“全局 + 每接口”双口径复核。

**结论（本路径下）**：

1. **本地 PC → 公网 HTTPS** 条件下，当前数据表现为：`G1-A` 不通过、`G1-B` 部分通过（`5/10/40`）、`G1-S` 不通过。
2. **对外承诺建议**：短期可将“稳定达到 B 档的并发范围”作为保守容量表述；若目标是 A/S，需继续做网关与应用调优并复测。
3. **口径管理**：后续若需 S 档背书，应增加“同地域 ECS -> 云服务”的对照测，避免将公网端到端长尾与系统内生瓶颈混为一谈。

---

## 8. SLA 对比结论

### 8.1 第 7 章高并发只读基线（threads=80，本地后端 + 远程数据库）

按本报告第 6.2 节约定 SLA：

- 错误率 `< 0.5%`：**未达标**（当前 2.36%）
- 核心读接口 P95 `< 300ms`：**未达标**（当前约 1.16s~1.53s）
- 稳定性（15 分钟无持续性抖动）：**未达标**（存在明显超时与长尾）

结论：该配置下可运行，但**可靠性和尾延迟均不满足**上述宽松 SLA。

### 8.2 G1 分级（见性能指标标准）

- **G1-A**（错误率 ≤0.5%、G1 P95≤300ms、G1 P99≤1500ms）：在第 7.5 节同一路径下，当前 `5/10/20/40/60/63` 均未通过，主要短板为 P95。
- **G1-B**（错误率 ≤2.0%、G1 P95≤800ms、G1 P99≤3000ms）：当前通过档位为 `5/10/20/40`，不通过档位为 `60/63`。
- **G1-S**（错误率 ≤0.1%、G1 P95≤200ms、G1 P99≤800ms）：在第 7.5 节同一路径下未出现通过档位。

### 8.3 小结

当前证据支持：**端到端公网体验的首要矛盾在链路长尾与连接层**；应用与网关优化仍有价值，但 **G1-S 不宜再用本机压公网的结果宣称**。

---

## 9. 优化建议（按优先级）

### 9.0 约束前提（本轮决策）

当前资源约束为：**短期无法提升公网带宽峰值**。因此网络层优化策略以“**连接复用、传输压缩、缓存前移、限流保护**”为主，目标是在既有带宽下提升有效吞吐并降低超时与长尾。

### 9.1 网络层优化（不增加带宽）

1. **连接复用优先（Keep-Alive）**
  - 客户端到 Nginx、Nginx 到应用均保持长连接，减少频繁建连与 TLS 握手开销。
  - 目标是降低 `ConnectTimeout` 比例与握手放大带来的尾延迟。
2. **传输压缩（Gzip/Brotli）**
  - 对 G1 只读 JSON 接口启用压缩（优先 `post/list`、`post/get` 等大响应）。
  - 在带宽不变时，通过“减少字节数”提升等效吞吐。
3. **缓存前移（网关层）**
  - 在 Nginx/CDN 层对只读接口启用短 TTL（如 10~60s）与条件缓存（`ETag` / `Cache-Control`）。
  - 热点命中后减少回源请求，降低应用与数据库压力，抑制高峰长尾。
4. **响应体瘦身**
  - 列表接口裁剪非必要字段，减少单请求传输体积；对可延迟加载内容采用二次请求。
  - 通过减少单次响应大小提升带宽利用效率。
5. **温和限流与排队保护**
  - 入口做令牌桶限流与突发平滑，避免瞬时并发打满连接队列导致超时雪崩。
  - 在“可控拒绝”与“大量超时失败”之间优先前者，保障整体稳定性。
6. **TLS 成本优化**
  - 开启 TLS Session Resumption（session cache/ticket），减少全握手比例。
  - 优化协议与密码套件组合，降低 CPU 与时延抖动。

### 9.2 应用与数据层协同优化

1. **P0：网络与网关容量排查**
  - 排查服务器安全组、防火墙、Nginx `worker_connections`、`keepalive`、上游连接池设置。
  - 查看 Nginx `access/error` 中 499/502/504 占比和峰值时段。
  - 检查主机 TCP 参数（连接队列、端口复用、TIME_WAIT 累积）。
2. **P0：应用端连接与线程池校准**
  - 校准 Tomcat/Undertow 线程池、连接超时、队列深度。
  - 结合 JVM GC 日志确认是否存在停顿放大（特别是长尾窗口）。
3. **P1：数据库慢查询治理**
  - 将慢查询阈值临时设为 1s，抓取 TopN SQL 与执行计划。
  - 优先优化 `list/get` 高频链路索引与分页策略。
4. **P1：读链路缓存与降载**
  - 对 `post/list`、`post/get`、`links/list` 引入短 TTL 缓存。
  - 对热点文章详情增加二级缓存，降低数据库突发压力。
5. **P2：压测脚本与场景分层**
  - 保持只读基线脚本用于容量评估。
  - 将管理端和写场景单独分批测试，避免混合负载掩盖瓶颈。

### 9.3 建议落地顺序（低成本优先）

1. **第一阶段（快速见效）**：`Keep-Alive` + `gzip` + 网关短 TTL 缓存（micro-cache）
2. **第二阶段（持续降尾）**：响应体瘦身 + Nginx 连接/超时参数校准
3. **第三阶段（稳定性保护）**：限流与排队保护 + TLS 会话复用优化

每阶段完成后执行一轮 `hold_sec=900` 同口径复测，对比错误率、P95/P99 与超时类型占比，再决定是否进入下一阶段。

---

## 10. 下一轮回归计划（可执行）

### 10.1 阶梯压测

按 4 轮执行，每轮 15 分钟：

1. `threads=20`
2. `threads=40`
3. `threads=60`
4. `threads=80`

每轮都输出独立 `jtl` 与官方 HTML 报告（`-e -o`）。

### 10.2 达标判定

- 错误率稳定 `<0.5%`
- 各核心接口 P95 `<300ms`
- 全局 P99 明显低于 5s 且无长尾峰值

### 10.3 回归后产出

- 更新本报告第 7~10 章
- 附每轮官方 Dashboard 截图（APDEX、响应分布、Errors、Over Time）
- 给出“最大可承载并发（在 SLA 内）”结论

### 10.4 当前分级与压测专项计划（已根据 7.5 节更新）

**G1-S（本地 → 公网）**：第 7.5 节已覆盖 **5～63** 线程；**本路径下不再追加「找 S 档最大并发」的阶梯任务**，除非网络或部署形态发生变化。

**建议的后续工作（择一或并行）**：

1. **同地域 ECS → 云服务**：按 `AyeezBlog-性能指标标准.md` 第 5.3 节，在云上起压测机，对 **G1-S / G1-A 最大并发** 做“同地域对照定标”（与当前本地 JMeter 结果分章书写）。
2. **公网体验（G1-A）**：若仍以本地为压测端，则以 **错误率 <0.5%、核心接口 P95<300ms** 为目标，在 **20～40** 线程等区间做 **重复长稳态** 验证，并记录是否偶发超标。
3. **网关与应用**：延续第 9 节 P0/P1 项，降低连接超时与尾延迟，为换压测源后的复测创造条件。

