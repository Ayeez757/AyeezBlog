<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.ayeez</groupId>
        <artifactId>AyeezBlog-Backend</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>blog-plugin-demo</artifactId>
    <name>blog-plugin-demo</name>
    <description>Day6 external plugin demo module</description>

    <properties>
        <plugin.host.classes>${project.basedir}/../blog-server/target/classes</plugin.host.classes>
        <plugin.host.jar>${project.basedir}/../blog-server/target/blog-server-0.0.1-SNAPSHOT-plain.jar</plugin.host.jar>
    </properties>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgs>
                        <arg>-classpath</arg>
                        <arg>${plugin.host.classes}${path.separator}${plugin.host.jar}</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>prepare-classes-double</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <copy todir="${project.build.directory}/classes-double">
                                    <fileset dir="${project.build.directory}/classes"/>
                                </copy>
                                <!-- -double.jar 的 plugin.properties 仅此一处维护，避免 resources 下并列第二份 properties -->
                                <echo file="${project.build.directory}/classes-double/plugin.properties"
                                      encoding="UTF-8">plugin.id=double-page-size-rule${line.separator}plugin.class=cn.ayeez.blogplugindemo.paging.DoublePageSizeRulePlugin${line.separator}plugin.version=1.0.0${line.separator}</echo>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                    </archive>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-double</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <classifier>double</classifier>
                            <classesDirectory>${project.build.directory}/classes-double</classesDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
